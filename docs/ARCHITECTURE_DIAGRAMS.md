# Visual Architecture Diagrams

Complete visual guide to the AI Newsletter system.

---

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      main.py (Entry Point)                   │
│  - Loads .env (API keys)                                    │
│  - Initializes Groq client                                  │
│  - Calls main_youtube_pipeline()                            │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              YouTubePipeline (Orchestrator)                  │
│  - Coordinates all components                               │
│  - Manages error handling                                   │
│  - Returns daily digest JSON                                │
└────┬─────────────────────────────┬─────────────────┬────────┘
     │                             │                 │
     ▼                             ▼                 ▼
┌──────────────┐         ┌──────────────┐   ┌────────────────┐
│SourceTracker │         │YouTubeVideoF │   │  TranscriptF   │
│              │         │inder + Groq  │   │  etcher        │
│ CSV Loading  │         │              │   │                │
│ Channel List │         │ YouTube API  │   │ Transcript API │
└──────────────┘         │ Groq API     │   │ TextCleaner    │
                         └──────────────┘   └────────────────┘
                                │                    │
                                ▼                    ▼
                         ┌──────────────┐   ┌────────────────┐
                         │ GroqNewsWriter│   │  JSONBuilder   │
                         │              │   │                │
                         │ Summarization│   │ JSON Output    │
                         │ 512 tokens   │   │ Format Digest  │
                         └──────────────┘   └────────────────┘
                                │                    │
                                └────────┬───────────┘
                                         ▼
                         ┌──────────────────────────┐
                         │  daily_digest.json       │
                         │  (Final Output)          │
                         │  - Date                  │
                         │  - Count of items        │
                         │  - Summarized content    │
                         └──────────────────────────┘
```

---

## 🔄 Data Flow

```
YouTube Channels (youtube_sources.csv)
       │
       ├─► Channel ID 1
       │    │
       │    ├─► YouTube API (find videos)
       │    │    └─► Find videos from last 24h
       │    │         │
       │    │         ├─► Video 1
       │    │         │    │
       │    │         │    ├─► Transcript API
       │    │         │    │    └─► Get captions
       │    │         │    │
       │    │         │    ├─► TextCleaner
       │    │         │    │    └─► Clean text
       │    │         │    │
       │    │         │    ├─► Groq LLM
       │    │         │    │    └─► Summarize (512 tokens)
       │    │         │    │
       │    │         │    └─► JSONBuilder
       │    │         │         └─► Create item
       │    │         │
       │    │         └─► Video 2, Video 3, etc.
       │    │
       │    └─► All videos processed
       │
       ├─► Channel ID 2
       │    └─► (same process)
       │
       └─► Channel ID N
            └─► (same process)
                    │
                    ▼
            daily_digest.json
            (All items combined)
```

---

## 📁 Folder Hierarchy

```
AI_NewsLetter/
│
├─ src/                          [Main Application Package]
│  │
│  ├─ models/                    [API Clients & Domain Objects]
│  │  ├─ groq_news_writer.py     ← Groq LLM (NEW)
│  │  ├─ youtube_finder.py       ← YouTube API v3
│  │  ├─ transcript_fetcher.py   ← Transcript API
│  │  ├─ source_tracker.py       ← CSV Management
│  │  └─ __init__.py
│  │
│  ├─ utils/                     [Reusable Utilities]
│  │  ├─ text_cleaner.py         ← Text Processing
│  │  ├─ json_builder.py         ← JSON Serialization
│  │  └─ __init__.py
│  │
│  ├─ pipelines/                 [Orchestration & Workflows]
│  │  ├─ youtube_pipeline.py     ← Main YouTube Workflow
│  │  └─ __init__.py
│  │
│  ├─ config/                    [Configuration - Reserved]
│  │  └─ __init__.py
│  │
│  └─ __init__.py
│
├─ docs/                         [Comprehensive Documentation]
│  ├─ README.md                  ← Documentation Index
│  ├─ SETUP_GUIDE.md             ← Quick Start
│  ├─ ARCHITECTURE.md            ← System Design
│  ├─ API_REFERENCE.md           ← API Methods
│  ├─ PROJECT_STRUCTURE.md       ← Folder Organization
│  ├─ TROUBLESHOOTING.md         ← Problem Solving
│  ├─ QUICK_REFERENCE.md         ← Cheat Sheet
│  └─ VERIFICATION.md            ← Testing Guide
│
├─ .env                          [API Keys & Config - SECURE]
├─ main.py                       [Entry Point - UPDATED]
├─ requirements.txt              [Dependencies - UPDATED]
└─ youtube_sources.csv           [Channel Configuration]
```

---

## 🔌 Component Dependencies

```
                    ┌─────────────────┐
                    │   main.py       │
                    │  (Entry Point)  │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  Load .env      │
                    │  (API Keys)     │
                    └────────┬────────┘
                             │
                    ┌────────▼─────────────┐
                    │ YouTubePipeline     │
                    │ (Orchestrator)      │
                    └────┬────────────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌─────────┐   ┌──────────┐   ┌──────────┐
    │ Models  │   │  Utils   │   │Pipelines │
    │         │   │          │   │          │
    │ ·Source │   │·TextClnr │   │·YouTube  │
    │  Tracker│   │·JSONBldr │   │  Pipeline│
    │         │   │          │   │          │
    │ ·YouTube│   └──────────┘   └──────────┘
    │  Finder │
    │         │
    │ ·Transcrpt
    │  Fetcher│
    │         │
    │ ·Groq   │
    │  Writer │
    └─────────┘

FLOW:
    main.py
        ↓
    Load .env
        ↓
    YouTubePipeline
        ├─ SourceTracker (get channels)
        ├─ YouTubeVideoFinder (find videos)
        ├─ TranscriptFetcher (get transcripts)
        ├─ TextCleaner (clean text)
        ├─ GroqNewsWriter (summarize)
        └─ JSONBuilder (format output)
            ↓
        daily_digest.json
```

---

## 🔑 Import Chain

```
main.py
    │
    ├─► .env (python-dotenv)
    │   └─ GROQ_API_KEY
    │   └─ YOUTUBE_API_KEY
    │
    └─► src.pipelines
        │
        └─► youtube_pipeline.py
            │
            ├─► src.models.source_tracker
            │   └─ loads youtube_sources.csv
            │
            ├─► src.models.youtube_finder
            │   └─ youtube-api-python-client
            │
            ├─► src.models.transcript_fetcher
            │   └─ youtube-transcript-api
            │
            ├─► src.models.groq_news_writer
            │   └─ openai (client for Groq)
            │       └─ https://api.groq.com/openai/v1
            │
            ├─► src.utils.text_cleaner
            │   └─ (no external deps)
            │
            └─► src.utils.json_builder
                └─ (no external deps)
```

---

## 📊 Processing Pipeline Detail

```
┌─────────────────────────────────────────────────────────┐
│              START: main.py                              │
│              Load .env, Call Pipeline                    │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  YouTubePipeline.__init__()                             │
│  - Initialize SourceTracker (CSV)                       │
│  - Initialize YouTubeFinder (YouTube API)               │
│  - Initialize TranscriptFetcher                         │
│  - Initialize GroqNewsWriter (Groq API)                 │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  process_all_youtube_sources()                          │
│  - Get all active sources from CSV                      │
│  - Iterate through each channel                         │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  For Each Channel:                                      │
│                                                          │
│  1. find_new_videos(channel_id, hours=24)              │
│     ├─ Call YouTube API                                 │
│     ├─ Get videos from last 24 hours                    │
│     └─ Return: List[Video]                              │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  For Each Video:                                        │
│                                                          │
│  2. fetch_and_clean(video_id, title, link)             │
│     ├─ Call Transcript API                              │
│     ├─ Clean HTML/timestamps                            │
│     └─ Return: Dict with clean text                     │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  3. summarize(title, text)                             │
│     ├─ Call Groq API                                    │
│     ├─ Generate 5-6 line summary                        │
│     ├─ Max 512 tokens                                   │
│     └─ Return: Summary text                             │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  4. build_item(type, title, summary, link)             │
│     ├─ Create JSON item                                 │
│     ├─ Keep original YouTube link                       │
│     └─ Return: Dict with item structure                 │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  Collect All Items                                      │
│  - Group by day                                         │
│  - Add metadata (date, count)                           │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│  build_daily_digest(items)                             │
│  - Create JSON with items                               │
│  - Add timestamp                                        │
│  - Save to file                                         │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│              END: daily_digest.json                      │
│              Return result summary                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 API Integration Points

```
┌──────────────────────────────────────────────────────┐
│              Application Layer                       │
│              (YouTubePipeline)                       │
└──────────────────────────────────────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌──────────────────┐  ┌──────────────┐  ┌─────────────┐
│   YouTube API    │  │ Transcript   │  │ Groq API    │
│   v3             │  │ API          │  │ (via OpenAI)│
│                  │  │              │  │             │
│ ·Search Videos   │  │ ·Get         │  │ ·Create     │
│ ·Get Metadata    │  │  Transcripts │  │  Completion │
│ ·Channel Info    │  │              │  │ ·Summarize  │
└──────────────────┘  └──────────────┘  └─────────────┘
      │                    │                   │
      ▼                    ▼                   ▼
  ┌─────────┐         ┌─────────┐        ┌──────────┐
  │YouTube  │         │Unofficial      │Groq       │
  │Python   │         │YouTube API     │OpenAI     │
  │Client   │         │Wrapper         │Client     │
  └─────────┘         └─────────┘      └──────────┘
```

---

## 📦 Data Structures

```
CSV Row:
┌─────────────────────────────────────┐
│ SourceTracker.load_sources()        │
├─────────────────────────────────────┤
│ source_id: "yt_001"                 │
│ type: "youtube"                     │
│ name: "Tech Channel"                │
│ url_or_id: "UCxxxxxxxxxxxxxxxxxxxxxx│
│ category: "tech"                    │
│ last_checked: "2026-01-22T12:00:00Z"│
└─────────────────────────────────────┘
                │
                ▼
YouTube API Response:
┌─────────────────────────────────────┐
│ YouTubeVideoFinder.find_new_videos()│
├─────────────────────────────────────┤
│ video_id: "jG7aFZu_I-0"             │
│ title: "AI Explained"               │
│ published_at: "2026-01-22T..."      │
│ link: "https://youtube.com/..."     │
└─────────────────────────────────────┘
                │
                ▼
Transcript:
┌─────────────────────────────────────┐
│ TranscriptFetcher.fetch_and_clean() │
├─────────────────────────────────────┤
│ video_id: "jG7aFZu_I-0"             │
│ title: "AI Explained"               │
│ clean_text: "Today we discuss AI... │
│ link: "https://youtube.com/..."     │
└─────────────────────────────────────┘
                │
                ▼
Groq Summary:
┌─────────────────────────────────────┐
│ GroqNewsWriter.summarize()          │
├─────────────────────────────────────┤
│ title: "AI Explained"               │
│ summary: "This video covers... (512 │
│ tokens max, 5-6 lines)"             │
└─────────────────────────────────────┘
                │
                ▼
JSON Item:
┌─────────────────────────────────────┐
│ JSONBuilder.build_item()            │
├─────────────────────────────────────┤
│ {                                   │
│   "type": "youtube",                │
│   "title": "AI Explained",          │
│   "summary": "This video covers...",│
│   "link": "https://youtube.com/..." │
│ }                                   │
└─────────────────────────────────────┘
                │
                ▼
Daily Digest:
┌─────────────────────────────────────┐
│ JSONBuilder.build_daily_digest()    │
├─────────────────────────────────────┤
│ {                                   │
│   "date": "2026-01-22",             │
│   "count": 5,                       │
│   "items": [                        │
│     {...}, {...}, {...}             │
│   ]                                 │
│ }                                   │
└─────────────────────────────────────┘
                │
                ▼
        daily_digest.json
```

---

## 🚀 Deployment Flow

```
Developer
    │
    ├─ python main.py
    │
    ├─ Load .env (Groq & YouTube keys)
    │
    ├─ YouTubePipeline processes:
    │   ├─ YouTube channels from CSV
    │   ├─ Find videos from last 24h
    │   ├─ Get & clean transcripts
    │   ├─ Summarize with Groq
    │   └─ Format as JSON
    │
    └─ Output: daily_digest.json
           │
           ├─ Schedule daily (cron)
           ├─ Archive JSON files
           ├─ Email digest
           └─ Upload to web
```

---

## 🔐 Security Model

```
┌────────────────────────────────────┐
│         Sensitive Data             │
│    (API Keys, Credentials)         │
└────────────┬───────────────────────┘
             │
             ├─ .env file
             │  └─ GROQ_API_KEY
             │  └─ YOUTUBE_API_KEY
             │
             ├─ .gitignore excludes .env
             │  └─ Never committed to git
             │
             ├─ python-dotenv loads at runtime
             │  └─ Available as os.getenv()
             │
             └─ main.py uses environment vars
                └─ Never in source code
```

---

**Visual diagrams for complete understanding! 📊**
